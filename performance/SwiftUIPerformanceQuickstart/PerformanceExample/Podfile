# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PerformanceExample (iOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PerformanceExample (iOS)
  pod 'Firebase'
  pod 'FirebasePerformance'

end

target 'Tests_iOS' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PerformanceExample (iOS)
  pod 'Firebase'
  pod 'FirebasePerformance'

end

target 'PerformanceExample (tvOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PerformanceExample (tvOS)
  pod 'Firebase'
  pod 'FirebasePerformance'

end
